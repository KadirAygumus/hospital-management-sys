package Interfaces;

// we added a useless interface to just have an interface, 
// new creative interfaces will arise during building GUI part we hope
public interface PersonType {

	public String returnType();
}